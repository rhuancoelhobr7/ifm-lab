# DADOS: versionados por decisão de Rhuan (2026-07-16) — o repo é compartilhado
# com um colaborador e TODO o data/ das pesquisas entra no git (raw, parquet,
# sealed, cache), estendendo a exceção de 2026-07-15 que cobria só o raw/.
# A regra 4 do CLAUDE.md registra a exceção. O bloco selado continua protegido
# pelos scripts (recusam data/sealed/ até o E11) — versionar não muda isso.
*.hst
*.tks

# Python
__pycache__/
*.pyc
.venv/
venv/
.pytest_cache/
.ipynb_checkpoints/

# Compilados MQL5
*.ex5

# Sistema / editores
.DS_Store
Thumbs.db
*.swp
